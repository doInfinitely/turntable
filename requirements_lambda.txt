# Requirements for Lambda GPU reconstruction
torch>=2.0.0
torchvision
numpy<2  # NumPy 1.x required for PyTorch compatibility
opencv-python
matplotlib
pillow

# Optional (for viewer only):
# pygame
# PyTorch3D - install separately with:
# pip install "git+https://github.com/facebookresearch/pytorch3d.git@stable"

